name: Linter
on: [push, pull_request]
jobs:
  
  # Run Ruff linter 
  ruff:
    name: Ruff Linter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: astral-sh/ruff-action@v3